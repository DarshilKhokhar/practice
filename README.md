Follow below steps to run the projec:

- npm install
- copy .env.example file and create .env file and add values for all the variables mentioned in the example file(dbUrl, dbname, etc)
- npm start


## To build the project
- npm run build

## To run the project
- npm run api